# Password Generator (Python)

Проект без зависимостей: один файл `script.py` генерирует пароль из букв, цифр и символов.

## Запуск

Передайте длину пароля одним аргументом командной строки:

```bash
python script.py 12
```

Скрипт выведет сгенерированный пароль в stdout.